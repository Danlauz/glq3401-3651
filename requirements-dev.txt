# Environnement de développement complet pour geostat_polymtl.
# Usage :  pip install -r requirements-dev.txt
# Préférer l'installation depuis pyproject : pip install -e .[dev,all]

# Lib elle-même en mode éditable
-e .[dev,all]
